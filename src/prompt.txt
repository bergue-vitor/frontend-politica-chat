You are a senior frontend architect.

I have a UI design with multiple screens (admin dashboard, documents, chat, login, register, timeline).

Your task is NOT to write full code.

Your goal is ONLY to design the project structure and architecture.

---

1. Analyze the UI and identify:

- All pages (full screens)
- All reusable components
- Shared layouts
- Logical groupings of components

---

2. Define a clean and scalable folder structure for a React + TypeScript + Tailwind project.

Use this pattern:

src/
 ├── pages/
 ├── components/
 ├── layouts/
 ├── data/
 ├── hooks/ (if needed)

---

3. For EACH folder, list:

- File names
- Purpose of each file (short explanation)

Example:

pages/
- AdminUsers.tsx → page responsible for user management screen
- Login.tsx → authentication screen

components/
- Sidebar.tsx → navigation sidebar
- Table.tsx → reusable table component

---

4. Identify reusable components and group them properly:

- layout components (Sidebar, Header)
- UI components (Button, Badge, Input)
- feature components (ChatMessage, DocumentCard, TimelineItem)

---

5. Define how components should be reused:

Explain relationships like:
- which pages use which components
- which components depend on others

---

6. DO NOT generate full code.

You may show:
- component signatures (optional)
- props examples (optional)

BUT DO NOT write full implementations.

---

7. Focus on:

- scalability
- clean separation of concerns
- reusable architecture
- real-world project standards

---

8. Output format:

- Folder tree
- Explanation of each part
- Component breakdown
- Page mapping

---

IMPORTANT:
Do NOT generate UI code.
Do NOT create full components.
Focus only on structure and architecture.